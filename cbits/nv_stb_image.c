/*
 * nv_stb_image — stb_image v2.30 implementation (vendored).
 *
 * Warnings suppressed: this is external code compiled under our
 * strict -Werror flags.  Only this file includes the implementation.
 * Other modules include stb_image.h for declarations only.
 */

#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wall"
#pragma GCC diagnostic ignored "-Wextra"
#pragma GCC diagnostic ignored "-Wpedantic"
#endif

#define STB_IMAGE_IMPLEMENTATION
#include "stb_image.h"

#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
